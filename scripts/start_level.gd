extends Node3D


# Called when the node enters the scene tree for the first time.
#func _ready() -> void:
	#(%EndLevel as EndLevel).body_exited.connect(
		#func(body: Node3D) -> void:
			#if body is Player:
				#(%Door as Door).close()
	#)
